import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryGreen = Color(0xFF00B14F); // Verde principal
  static const Color lightGreen = Color(0xFF8CE99A);   // Verde suave para fondos o acentos
  static const Color accentOrange = Color(0xFFFFA726); // Para elementos destacados (ej. precios)
  static const Color background = Color(0xFFF9F9F9);   // Fondo general claro
  static const Color textPrimary = Color(0xFF212121);  // Texto principal (negro suave)
  static const Color textSecondary = Color(0xFF757575); // Texto secundario (gris)
  static const Color border = Color(0xFFE0E0E0);        // Bordes y divisores
  static const Color white = Colors.white;              // Blanco puro
}